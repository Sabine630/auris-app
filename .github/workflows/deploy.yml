name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      # 第三方 action 一律釘 commit SHA（防 tag 被改指到惡意 commit 的供應鏈攻擊）；
      # 註解版號供人讀，Dependabot（github-actions ecosystem）會自動開 PR 更新
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: 'auris-vue/package-lock.json'

      - name: Check project config
        run: node scripts/check-project-config.mjs
          
      - name: Install dependencies
        working-directory: ./auris-vue
        run: npm ci

      - name: Audit dependencies
        working-directory: ./auris-vue
        run: npm audit --audit-level=high

      - name: Run tests
        working-directory: ./auris-vue
        run: npm test
        
      # 角色語音（批次 A／B 已在 dev，C／D 未施工）不隨正式版發布：旗標關閉時
      # Rollup 會把 VoiceSettingsView 與 services/tts 整個從產物移除。dev 的 Vercel
      # 測試版不設此變數，預設為開、繼續驗證。語音要正式上線時刪掉這兩行即可。
      - name: Build
        working-directory: ./auris-vue
        env:
          VITE_VOICE_ENABLED: '0'
        run: npm run build
        
      - name: Setup Pages
        uses: actions/configure-pages@1f0c5cde4bc74cd7e1254d0cb4de8d49e9068c7d # v4.0.0

      - name: Upload artifact
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3.0.1
        with:
          path: './auris-vue/dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
