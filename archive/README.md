# Archive

此目錄保存 Vue 重構前的歷史單檔原型，只供追溯 UI 與舊行為。

- 不修改、不建置、不部署。
- 內容可能包含已淘汰的硬編碼密碼、舊 API 邏輯或不符合目前資安標準的實作。
- 任何出現在 archive 或 Git 歷史中的密碼都必須視為公開且失效，不能用於目前環境。
- 現行實作以 `auris-vue/src/` 與 `auris-vue/ARCHITECTURE.md` 為準。
