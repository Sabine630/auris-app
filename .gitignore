node_modules/
.DS_Store
.env
Auris 使用說明.md
使用說明截圖/
# 共用 hooks 在 scripts/hooks/；Claude/Codex 的 skills 與專案設定要進版控，只排除個人本機設定
.claude/settings.local.json

# 正式版由 GitHub Actions 自 auris-vue/dist 部署，專案根目錄不再放 build 成品（P97-P100 整理批次）。
# 防止舊習慣 `cp -r dist/* ..` 意外把死成品重新加回版控。
/assets/
/index.html
/404.html
/favicon.svg
/icons.svg
