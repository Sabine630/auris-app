# Auris 專案健檢與資安審查報告

- 審查日期：2026-07-15
- P113 復驗日期：2026-07-15
- P114 工作區復驗日期：2026-07-15（待提交）
- P127 工作區復驗日期：2026-07-18（待提交）
- 正式版：`main` / `P110` / `6c066991002bb6e780c7f4dd32ecf678866ed4a7`
- 首次審查開發版：`dev` / `P112` / `d9d68b0de4c2c8e39be77c867666903b1c8c832c`
- P113 修復 commit：`3a6fea53d9ff881634593276e1930ed1feea1030`
- 復驗時 dev HEAD：`57731357db85ca21f61bdc67c703b9ead1035874`
- 審查範圍：功能健康、資料完整性、XSS／注入、API 憑證、匯入／匯出、診斷資訊、依賴、CI/CD、GitHub 分支防線與實際部署
- 首次審查結論：未發現 Critical；1 項高優先資料完整性問題、4 項中優先安全／維運問題，另有數項低優先強化與工程品質缺口
- 最新結論：P113 已修復備份原子性、API endpoint／既有 key 組合與外部追蹤圖片；P114 修復 Vertex token cache、診斷原始錯誤文字與主題預讀；P127 再完成圖片格式／容量、JSON 資源限制與 v1 schema validator。GitHub 遠端 required checks 與 PR 發布流程仍待處理。

> 本報告已分開核對正式版 P110 與 dev P112。`db.js`、`api.js`、`llm.js`、`diag.js`、`format.js`、CI、部署設定在兩版之間沒有差異，因此核心發現同時適用 P110 與 P112。

## 目前狀態表（交接時先看這裡）

| 編號 | 發現 | 初始等級 | 目前狀態 | 修復／下一步 |
|---|---|---:|---|---|
| SEC-01 | 全站備份匯入非原子操作 | High | ✅ P113 已修復 | `3a6fea5`；130 tests 通過 |
| SEC-02 | 備份可改寫 endpoint 並搭配目的端既有 key | Medium | ✅ P113 已修復 | 四項 API 設定不匯出、匯入忽略 |
| SEC-03 | Vertex token cache 未綁定 service account | Medium | ✅ P114 已修復（工作區待提交） | 身分含 project/email/key 指紋；`expires_in`＋安全緩衝；4 tests |
| SEC-04 | GitHub 分支缺少強制遠端防線 | Medium | 🟡 部分完成 | main 對非管理者已禁 force push／刪除（API 復驗）；`enforce_admins=false` 管理者可繞過；required CI 仍關閉（需先改 PR 發版流程）；dev 未保護 |
| SEC-05 | 匯入聊天可載入外部追蹤圖片 | Medium | ✅ P113 已修復 | 三條匯入路徑移除外部 URL |
| SEC-06 | Claude/Codex hooks 重複且 Codex 綁死絕對路徑 | Medium | ✅ 已修復（工作區待提交） | 共用腳本移至 `scripts/hooks/`；設定、語法與行為測試通過 |
| HARD-01 | 診斷可能保存第三方錯誤文字 | Low | ✅ P114 已修復（工作區待提交） | 信任分級：第三方／來源不明一律 strict 分類；本地錯誤經遮蔽管線後保留；schema 2 結構化，讀出時重驗證與重遮蔽 |
| HARD-02 | 匯入 schema、MIME 與資源限制不足 | Low | ✅ P127 已修復 | 共用驗證層＋226 tests |

> 下方 §2–§10 保留首次審查的原始證據，不代表目前仍可重現；最新驗收以本表與 §14 為準。

---

## 1. 驗證摘要

### 已通過

- Vitest：12 個測試檔、116/116 通過
- Vite production build：成功
- `npm audit --audit-level=high`：0 vulnerabilities
- dev 最新 GitHub Actions CI：成功
- main 最新 GitHub Pages deployment：成功
- 本機 dev P112 抽查聊天、回憶、通知與設定頁：無 console error
- 正式站實際顯示 P110，設定頁載入無 console error
- XSS 主路徑：所有動態 `v-html` 均經 `formatContent()` escape；未找到可直接利用的 XSS
- 目前檔案與 Git 歷史的祕密特徵掃描：只命中 `sk-demo-...` 教學假金鑰，未找到真實 key
- 備份匯出確實排除 `api_key`

### 審查限制

- 未使用真實 API key 對第三方供應商做攻擊性測試，避免傳輸使用者資料與產生費用
- 未讀取使用者實際瀏覽器儲存內容
- 未做實機 iOS PWA 鍵盤／儲存清除測試
- 專案目前沒有 CodeQL、Semgrep、SAST workflow 或完整 E2E suite

---

## 2. [P113 已修復] 高優先：備份還原不是原子操作，失敗時會留下半套資料

### 證據位置

- `auris-vue/src/services/db.js:101-143`
- `auris-vue/src/views/SettingsView.vue:291-318`

### 問題 A：缺少 store 的備份仍會通過

目前驗證邏輯：

```js
const rows = jsonData.data[s];
if (rows == null) continue;
```

但驗證完成後會清空所有 store：

```js
for (const s of ALL_STORES) {
  await dbClear(s);
}
```

因此備份若缺少 `messages`，仍會被接受，但既有 `messages` 會被清空且不會還原。

### 問題 B：清空與還原分散在多個 transaction

`dbClear()` 與每次 `dbPut()` 都各自建立一個 IndexedDB transaction。若還原中途發生 quota、I/O 或資料寫入錯誤，先前的清空與部分寫入不會 rollback。

### 已重現結果

使用實際 `db.js` 搭配一次性 IndexedDB 替身測試：

1. 只含 `characters`、`settings` 的備份被判定匯入成功。
2. 原有 `messages` 被清空。
3. 模擬第二筆訊息發生 `QuotaExceededError`：
   - 新角色已寫入
   - 第一筆新訊息已寫入
   - 第二筆失敗
   - 舊資料已不存在
   - 原 API key 尚未走到補回步驟，結果為空
4. UI 只顯示「匯入失敗：檔案格式錯誤或損毀」，但資料庫已被部分改寫。

### 風險

- 使用者目前資料可能被部分清空
- App 重新載入後可能進入不一致狀態
- 備份檔雖仍存在，可重試，但目的端原資料已遭破壞

### 修復建議

1. 以單一 multi-store `readwrite` transaction 完成「清空＋還原」。
2. 任一筆失敗時 abort，讓 IndexedDB rollback 全部異動。
3. 備份版本需定義必填 store manifest；舊版本透過明確 migration 補空陣列，不可對任意缺失靜默 `continue`。
4. 驗證每個 store 的必要欄位與基本型別，不只檢查 `id`／`key` 是否存在。
5. 匯入前可先估算資料量與 Storage quota。
6. 錯誤訊息需區分格式錯誤、quota、transaction abort 與未知錯誤。

### 驗收條件

- [ ] 缺少必填 store 的同版備份會在清空前被拒絕
- [ ] 舊版備份透過 migration 明確補齊 store
- [ ] 模擬第 N 筆 `put` 失敗後，目的端所有原資料完整保留
- [ ] 匯入成功後所有 store 與備份內容一致
- [ ] 原 key 的保留／清除策略有獨立測試
- [ ] 為 `exportAllData()`、`importAllData()` 新增完整單元測試

---

## 3. [P113 已修復] 中優先：備份可改寫 API endpoint，再搭配目的端既有 key

### 證據位置

- `auris-vue/src/services/db.js:85-143`
- `auris-vue/src/services/llm.js:24-29`
- `auris-vue/src/services/llm.js:225-257`

### 精確適用條件

備份檔本身不含 API key。風險只在以下條件同時成立時存在：

1. 在同一 origin 匯入
2. 目的端匯入前已存有 API key
3. 匯入不可信或遭修改的備份
4. 備份指定新的 `api_base`／`api_provider`

跨正式站與 Vercel、新裝置、新瀏覽器，或目的端原本沒有 key 時，不成立。

### 已重現結果

- 匯出資料中沒有 `api_key`
- 匯入含 `api_base: https://evil.example/v1` 的備份
- 匯入後目的端原 key 被保留
- `api_base` 已變成備份指定值

### 修復建議

- 匯入時忽略 `api_key`、`api_base`、`api_provider`、`api_model`
- 或將 API 設定視為一組不可拆分的本機設定
- 若 endpoint／provider 改變，清除現有 key 並要求使用者重新輸入
- 自訂 endpoint 至少驗證為合法 HTTPS URL，並在儲存前顯示目標 hostname

### 驗收條件

- [ ] 匯入備份無法靜默改變現有 key 的傳送目的地
- [ ] endpoint 改變後，未重新授權前不會發送背景 AI 請求
- [ ] 同 origin、跨 origin、目的端無 key 三種情境都有測試

---

## 4. [P114 已修復，工作區待提交] 中優先：Vertex token 快取未綁定 service account

### 證據位置

- `auris-vue/src/services/api.js:25-71`

目前只有：

```js
let _vtok = null, _vtokExp = 0;
```

快取未記錄 `client_email`、`project_id` 或 private key 指紋。

### 已重現結果

使用兩個不同 service account 呼叫 `getVertexToken()`：

- 第一個帳號取得 `token-1`
- 第二個帳號也直接收到 `token-1`
- OAuth fetch 總共只執行一次
- 第二個帳號的 private key 完全沒有被使用

若使用者切換 Vertex 帳號，約 58 分鐘內可能繼續使用舊身分 token，造成權限、計費專案或請求失敗混淆。

> `cloud-platform` OAuth scope 本身不等於擁有整個 GCP 專案權限；實際權限仍由 service account IAM roles 決定。

### 修復建議

- 快取 key 至少包含 `client_email + project_id`
- 更嚴謹可加入 private key 公鑰指紋
- API 設定儲存／切換 provider 時主動清除 Vertex token cache
- 使用 OAuth 回應的實際 `expires_in`，不要固定 3,500,000 ms

### 驗收條件

- [ ] 同一 service account 在有效期限內共用 token
- [ ] 不同 service account 一定重新換 token
- [ ] 修改 service account JSON 後不會沿用舊 token
- [ ] token 到期後會重新取得

---

## 5. [部分完成] 中優先：GitHub 分支缺少強制遠端防線

### 已確認狀態

- `main protected: false`
- `dev protected: false`
- main、dev 最新 commit 都是 unsigned
- main push 會直接觸發 GitHub Pages 正式部署

本機 `guard-main-push` hook 有價值，但無法防止 GitHub UI、其他電腦、其他 Git client 或遭盜憑證直接推送。

### 修復建議

- 啟用 `main` branch protection／ruleset
- 禁止 force push 與刪除
- 要求 CI status check 成功
- main 僅允許 PR merge 或指定 release 流程
- 可一併保護 dev，至少要求 CI
- 評估啟用 signed commits

### 驗收條件

- [ ] GitHub API 顯示 main 已 protected
- [ ] 未通過 CI 無法合併／推入 main
- [ ] 一般帳號無法 force push main

---

## 6. [P113 已修復] 中優先：聊天匯入可載入外部追蹤圖片

### 證據位置

- `auris-vue/src/views/ChatRoomView.vue:78`
- `auris-vue/src/views/ChatRoomView.vue:1422-1439`

匯入會展開整個訊息物件：

```js
await dbPut('messages', { ...m, id: ..., charId });
```

畫面則直接載入：

```vue
<img v-if="m.image" :src="m.image">
```

不可信聊天 JSON 可放入外部 HTTPS tracking image。使用者開啟聊天時會向該主機發送請求，暴露 IP、時間與來源 origin。這不是 XSS。

### 修復建議

- 匯入時只接受預期格式，例如 `data:image/jpeg;base64,...`
- 非 data URL 圖片欄位直接移除
- 驗證 decoded MIME、base64 大小與像素上限

### 驗收條件

- [ ] 匯入含 `https://attacker.example/pixel` 的訊息不會產生網路請求
- [ ] 合法 Auris base64 圖片仍可正常還原
- [ ] 超大圖片會在寫入 IndexedDB 前被拒絕

---

## 7. [P114 已修復，工作區待提交] 低優先：診斷資訊可能包含第三方原始錯誤文字

### 證據位置

- `auris-vue/src/services/llm.js:136-141`
- `auris-vue/src/services/diag.js:13-22`
- `auris-vue/src/services/diag.js:70-75`

LLM 服務商回傳的錯誤訊息會原樣寫入 ring buffer，最多 300 字。

### 已重現結果

假 API 回傳：

```text
provider echoed conversation: 我的私人對話
```

診斷 ring buffer 隨即包含相同文字。因此正常供應商通常不會回傳對話內容，但程式碼無法保證「絕不含」。

### 修復建議

- 診斷只記 provider、model、HTTP status、內部分類碼
- 不直接保存第三方 response message，或使用嚴格 allowlist／遮罩
- UI 文案改成「設計上不主動包含對話與金鑰；分享前仍請檢查」

---

## 8. 低優先：匯入缺少資源限制

以下匯入路徑都沒有檔案大小、資料筆數與字串長度上限：

- 全站備份
- 單角色備份
- 聊天 JSON
- 個人／角色頭像
- 聊天圖片

風險主要是使用者主動選取異常大檔後，造成主執行緒卡死、記憶體耗盡或 IndexedDB quota 用完。

建議為每種匯入格式設定：檔案大小、筆數、單則文字、base64 圖片與總資料量上限。

---

## 9. 低優先：部署與 CI 防禦縱深

### 正式站

- 有 HSTS
- CSP 透過 HTML meta 生效
- 沒有 HTTP `frame-ancestors`／`X-Frame-Options`
- 沒有 `X-Content-Type-Options`、Referrer Policy、Permissions Policy 等應用層 header
- CSP 含 `script-src 'unsafe-inline'`
- `connect-src` 與 `img-src` 允許所有 HTTPS，這與自訂 API／圖片需求有關，但降低 CSP 限制力

GitHub Pages 不方便提供自訂 response headers；若要完整 header 防線，需要 Cloudflare／自有 hosting／其他可控 CDN。

### CI/CD

- ~~CI 使用 `npm install`，建議改為 `npm ci`~~（2026-07-15 維運整理已改）
- ~~deploy workflow 只 build，沒有再次 test/audit~~（2026-07-15 維運整理已補）
- GitHub Actions 使用 `@v4`／`@v3` tag，未 pin commit SHA
- 專案內沒有 Dependabot config、CodeQL 或其他 SAST workflow

---

## 10. 工程健康與品質缺口

### 備份核心零測試

現有 `auris-vue/src/services/__tests__/backup.test.js` 只測：

- 備份提醒門檻
- 14 天週期
- snooze

沒有測試：

- `exportAllData()`
- `importAllData()`
- API key 排除與保留
- 缺 store
- 格式驗證
- transaction rollback
- quota／I/O 失敗
- 舊版 migration

這是本次高優先資料問題未被測試攔下的主要原因。

### 主題預讀使用舊 DB 名稱

- `auris-vue/index.html` 開啟 `AurisDB_v2`
- 實際 App DB 是 `auris`
- `SettingsView` 儲存主題時只寫 IndexedDB，沒有更新 `auris-theme` localStorage

可能造成：

- 冷啟動主題閃爍
- 建立無用的 `AurisDB_v2` 空資料庫
- 預讀程式無法讀到實際主題

### 其他品質項目

- `/lock` 目前是占位頁，不是實際 App Lock
- 沒有 lint script
- 沒有型別檢查
- 沒有 coverage gate
- 沒有正式 E2E suite
- 主 bundle 約 445 KB、gzip 約 146 KB；路由同步載入，後續可能繼續膨脹
- 本機 `node_modules` 有少量 extraneous 套件；改用 `npm ci` 可保持乾淨

---

## 11. 已確認安全設計

- 備份不匯出 API key
- demo 使用獨立 `auris-demo` IndexedDB
- demo LLM 呼叫被本地假回覆攔截，不外連
- 動態文字進 `v-html` 前會 escape `&`、`<`、`>`
- 沒有使用 `eval()`、`new Function()` 或動態 `document.write()`
- `innerHTML` 唯一使用處是固定的 DB 初始化錯誤畫面
- Vertex private key 透過 WebCrypto 以不可匯出的 CryptoKey 進行簽章
- npm 依賴掃描目前為 0 vulnerabilities
- Vercel dev deployment 目前有 SSO、HSTS、`X-Frame-Options: DENY` 與 noindex

---

## 12. 舊報告更正紀錄

| 舊結論 | 更正後 |
|---|---|
| 備份會造成高風險 key 外洩 | 備份本身不含 key；改為具嚴格前提的中風險 endpoint／既有 key 組合 |
| `cloud-platform` 等於整個 GCP 專案權限 | 錯誤；實際權限由 service account IAM roles 決定 |
| 明文 IndexedDB 是高風險漏洞 | 撤回；這是純前端 BYOK 架構限制，不等於已發現漏洞 |
| 匯入大小限制是中風險 | 降為低風險 |
| 安全標頭缺失是中風險 | 降為防禦縱深 |
| branch protection 缺失 | 保留，已由 GitHub API 再次確認 |
| 未發現備份資料完整性問題 | 已補正為本報告最高優先項目 |

---

## 13. 建議修復順序

1. ~~備份還原改為單一 transaction、補 schema／migration 驗證與測試~~（P113 核心完成；完整欄位型別驗證仍待補）
2. ~~API 設定不隨備份改寫，或 endpoint 改變時清 key~~（P113 完成）
3. ~~Vertex token cache 綁定 service account 身分~~（P114 完成；待提交）
4. **啟用 main required CI，並評估保護 dev**（main 已 protected，但 required checks 仍關閉；dev 未保護）
5. ~~過濾聊天匯入的外部圖片 URL~~（P113 完成；圖片 MIME／大小驗證仍待補）
6. ~~修正 `.codex/hooks.json` 的本機絕對路徑~~（2026-07-15 維運整理完成；待提交）
7. ~~縮減診斷錯誤內容~~（P114 完成；待提交）
8. ~~補匯入資源上限與 v1 schema validator~~（P127 完成）；主題 DB 預讀已於 P114 修正，CI 基礎強化已完成
9. 補 E2E、lint、coverage 與維護性整理

---

## 14. P113／P114 修復後復驗（2026-07-15）

### 14.1 復驗方式與結果

- 審核 `d9d68b0..3a6fea5` 的實際程式 diff，另確認 dev HEAD `5773135` 工作區乾淨。
- Vitest：13 個測試檔、130/130 通過。
- Vite production build：成功；主 bundle 約 446 KB、gzip 約 146 KB。
- `npm audit --audit-level=high`：0 vulnerabilities。
- GitHub Actions：P113 run `29419506149` 成功；最新 dev run `29421462073` 亦成功。
- 本機以 `/auris-app/settings?demo=1` 實測 P113 設定頁：版號與說明正確，console 無 warning/error。
- 再次掃描 tracked files 的 API key／私鑰特徵：僅命中 `sk-demo-xxxxxxxxxxxxxxxxxxxx` 教學假金鑰，未發現真實憑證。
- 版號與文件防呆：`APP_VERSION`、進度總覽 P113、ARCHITECTURE P113 排序與「當前版本」位置均正確。

### 14.2 已修復並通過驗收

#### A. 高優先：全站備份匯入原子性

位置：`auris-vue/src/services/db.js:122-178`、`auris-vue/src/services/__tests__/db.test.js`

- `importAllData()` 現在以單一涵蓋全部 store 的 `readwrite` transaction 執行清空、還原與本機 API 設定補回。
- 同版備份缺少 11 個原始必填 store 時，會在開啟寫入 transaction 前拒絕。
- `chat_memories`、`wishes`、`notes` 經 Git 歷史確認為備份功能上線後新增；舊備份缺少時視為空，歷史相容判斷合理。
- 寫入中途發生非法 IndexedDB key 時，測試證實清空與先前寫入全部 rollback，舊角色、舊訊息與本機 key 均完整保留。
- 成功匯入、匯出→匯入 round-trip、缺 store、錯誤版本、API 設定保留均已有測試。

結論：原報告第 2 節會破壞目的端原資料的高風險問題，已修復。

#### B. 中優先：備份改寫 API endpoint

位置：`auris-vue/src/services/db.js:91-114`、`auris-vue/src/services/db.js:144-155`

- `api_key`、`api_provider`、`api_base`、`api_model` 均不寫入備份。
- 匯入時即使備份惡意夾帶上述四項，也會全部忽略。
- 同一 origin、同一瀏覽器原有的四項 API 設定會在 transaction 內補回。
- 已有「惡意 api_base + 目的端既有 key」測試，確認 endpoint 不會被改寫。

結論：原報告第 3 節已修復。

API key 行為必須維持以下精確說明：

- 備份檔不包含 API key，也不包含 provider/base/model。
- 在同一網站 origin、同一瀏覽器且資料庫仍保有 API 設定時，匯入後沿用原設定，不需重新輸入。
- 換裝置、換 origin（例如 GitHub Pages 與 Vercel）、無痕環境或已清除網站資料時，目的端沒有可保留的 key，因此仍須重新輸入 API key，並重新選擇 provider/model/base；這是安全設計，不是匯入遺失。

#### C. 中優先：外部追蹤圖片

位置：`auris-vue/src/services/db.js:95-103`、`auris-vue/src/services/db.js:146`、`auris-vue/src/services/db.js:226`、`auris-vue/src/views/ChatRoomView.vue:1438`

- 全站備份、單角色備份、單聊天室匯入三條路徑都會移除非 `data:image/` 的 `image` 欄位。
- `https://attacker.example/pixel` 測試確認會被移除；合法 Auris `data:image/...` 圖片仍可 round-trip。

結論：原報告第 6 節的外部 HTTPS tracking image 風險已修復。

### 14.3 P114 後續修復與剩餘項目

#### [P2][P114 已修復，工作區待提交] Vertex token cache 跨 service account 共用

位置：`auris-vue/src/services/api.js:25-71`

原本只有模組級 `_vtok`、`_vtokExp`；命中快取時不比較 service account 身分。P114 改以 `project_id + client_email + private key SHA-256 指紋` 建立 cache identity，只保存不可逆指紋；不同帳號、project 或 key 不共用。

本次修復：

1. cache identity 同時涵蓋 `client_email`、`project_id` 與 private key 指紋。
2. 使用 OAuth `expires_in`，預留最多 60 秒／10% 安全緩衝，不再固定 3,500,000 ms。
3. token 失敗訊息只保留安全錯誤碼，不串接完整 OAuth response。
4. `api.test.js` 使用純假 service account／私鑰覆蓋四條 cache 行為。

驗收條件：

- [x] 不同 service account 連續呼叫時 OAuth fetch 執行兩次，第二個帳號不會拿到第一個 token
- [x] 相同 service account 在有效期內只 fetch 一次
- [x] 同帳號更換 private key 時重新取得 token
- [x] `expires_in` 到期／安全緩衝後會重新取得 token
- [x] 測試只含明確假 service account 與假私鑰

#### [P2] GitHub 分支防線僅部分完成

2026-07-15 GitHub API 復驗：

- `main protected: true`：相較首次審查已有改善。
- main 的 `required_status_checks.enforcement_level` 仍為 `off`，contexts/checks 為空，CI 不是合併／推入 main 的強制條件。
- `dev protected: false`。
- 最新 main 與 dev commit 仍是 unsigned；簽章屬可選強化，不列為阻擋項。

維運修復要求（需 GitHub repository admin 權限，不能只改本機程式碼）：

1. main ruleset 要求 CI workflow 成功後才能合併或推入。
2. 禁止 main force push 與刪除；確認規則同樣套用管理者或明確記錄 bypass 人員。
3. 建議 main 僅允許 PR／release 流程；dev 至少評估禁止 force push 並要求 CI。

驗收條件：

- [ ] GitHub API 顯示 main `protected: true`
- [ ] main required status checks 不再是 `off`，且包含實際 CI check
- [ ] 未通過 CI 的 commit 無法進入 main
- [ ] main 無法 force push／刪除

#### [P2][已修復，工作區待提交] Codex hooks 綁死單一本機路徑

位置：`.codex/hooks.json:9-21`

三個 hook command 都寫死：

```text
<使用者目錄>/<專案路徑>/.codex/hooks/...
```

repo 搬到其他資料夾、換電腦或由他人 clone 後，hook 會找不到腳本，使 main push 確認、版號檢查與 secret leak 掃描失效。路徑也不必要地揭露本機帳號名稱與目錄結構。

本次修復：

1. 三個共用腳本移至 `scripts/hooks/`，Claude 與 Codex 設定都指向同一份實作。
2. Codex 由 `git rev-parse --show-toplevel` 解析根目錄；腳本本身在沒有 `CLAUDE_PROJECT_DIR` 時也會用 Git 根目錄，不再依賴單一工具環境變數。
3. 已在目前含空白與非 ASCII 字元的專案路徑完成 shell 語法、設定 JSON 與 hook 行為測試。

驗收條件：

- [x] 設定不含個人絕對路徑，且由 Git 根目錄解析共用腳本
- [x] 版號與 secret hooks 在無相關 staged 異動時正常放行
- [x] 隔離暫存 repo 中，未版更的 `auris-vue/src/` commit 會被擋下
- [x] 隔離暫存 repo 中，staged 假 key 會被擋下，輸出只顯示檔名、不顯示 key 本體
- [x] 嘗試推 main 時仍會要求使用者確認；推 dev 不誤擋

#### [P3][P127 已修復] 圖片、備份 schema 與匯入資源限制

位置：`auris-vue/src/services/db.js:97-102`、`auris-vue/src/services/db.js:137-143`、三條匯入 UI

- `stripUnsafeImage()` 只檢查 `startsWith('data:image/')`，仍接受任意 image MIME、非 base64 data URL 與無大小上限內容。
- 全站備份只驗證 record 是 object 且存在 `id`／`key`，未驗證必要欄位型別、合理長度或跨欄位關係。
- 全站備份、單角色備份與聊天 JSON 都沒有檔案大小、筆數、單則文字、圖片與總資料量上限。
- 單角色匯入與聊天匯入仍是多個獨立 transaction；中途失敗不會破壞原資料，但可能留下部分新增資料。

修復要求：

1. 只允許實際支援的 raster MIME（例如 JPEG/PNG/WebP），驗證 `;base64,` 格式與解碼後大小；不要只相信宣告 MIME。
2. 在 `FileReader`／`file.text()` 前先檢查檔案大小；解析後再檢查筆數、文字長度、圖片大小與估算總量。
3. 為 v1 備份建立明確 schema／validator，至少驗證各 store keyPath 型別與核心欄位基本型別。
4. 評估將單角色與聊天匯入也改為各自單一 transaction，失敗時不留下半套新增資料。

P127 完成項目：

1. 新增 `services/importValidation.js`，三條入口在 `file.text()` 前分別限制完整備份 64 MB、角色備份 24 MB、聊天備份 32 MB。
2. 圖片只接受 JPEG／PNG／WebP `;base64,` data URL，核對 magic bytes；單張解碼後上限 5 MB，另限制各匯入類型圖片總量。外部 URL、SVG、非 base64、偽造 MIME、損毀與超限圖片在寫入前拒絕或移除。
3. JSON 增加各 store 筆數、總 records、單欄文字 200,000 字、文字總量、陣列長度、物件欄位數與巢狀深度上限。
4. v1 schema validator 驗證 store keyPath 為非空且不重複的字串，並驗證角色、訊息、內容資料、通知與群組等核心欄位型別。
5. 全站備份仍使用 P113 的單一 multi-store transaction；P127 沒有重做或削弱既有 rollback。單角色／聊天匯入原子化仍是低優先評估項目：失敗不破壞原資料，但可能留下部分新增資料。

驗收條件：

- [x] 外部 URL、SVG、非 base64 與超限圖片在寫入 IndexedDB 前被拒絕或移除
- [x] 合法 JPEG/PNG/WebP Auris 圖片仍可匯出→匯入
- [x] 超大 JSON、超多 records、超長單則文字會在合理時間內被拒絕
- [x] 格式拒絕與 quota／transaction 錯誤有可理解且不洩露敏感內容的訊息

#### [P3][P114 已修復，工作區待提交] 診斷隱私文案與實作不一致

位置：`auris-vue/src/services/diag.js:13-22`、`auris-vue/src/services/diag.js:36-45`、`auris-vue/src/views/SettingsView.vue` 診斷文案

原本第三方供應商若在 error message 回傳對話片段，global error／unhandled rejection 可能把前 300 字寫進 ring buffer；設定頁卻顯示「不含對話內容與金鑰」。

P114 最終版採「信任分級」：ring buffer 為 schema 2 結構化欄位；strict（預設，LLM／網路／unhandledrejection／來源不明）只留 provider/model、HTTP status 與安全分類，不保存原始 message；trusted-local（受控 call site——經 `new URL(filename).origin === location.origin` 驗證同源的 window runtime error、DB 初始化）保留經「刪控制字元→遮蔽金鑰→遮蔽 URL→截 300 字」的訊息。`getErrors()` 讀出時逐筆重驗證＋重遮蔽＋限 30 筆＋timestamp 重輸出 ISO（防 localStorage 竄改），舊版字串資料一律 strict 重分類；`formatDiagError()` 輸出前再做一道 allowlist／遮蔽。SettingsView 文案為「含環境資訊、錯誤分類與經遮蔽的本地錯誤資訊，不含對話內容與金鑰」。復查提出的探針（超量讀取、timestamp 注入、location query 殘留、控制字元拆金鑰、format 原樣輸出）均已補為正式測試並通過。

#### [P3] 其他工程與防禦縱深狀態

- P114 已將主題預讀改為既有 `auris` DB；不存在時 abort 防止建立空 schema，讀完關閉連線；`applyTheme()` 同步 `auris-theme` localStorage。
- CI 與 deploy 已改用 `npm ci`，並新增 tracked 設定可攜性檢查。
- deploy workflow 已補 `npm audit --audit-level=high` 與 `npm test`，通過後才 build。
- GitHub Actions 仍使用浮動 major tag，尚未 pin commit SHA。
- 尚無 Dependabot、CodeQL／SAST、lint、typecheck、coverage gate 或正式 E2E suite。
- `/lock` 仍是占位頁。

### 14.4 P114 工作區驗證結果

- Vitest：15 個測試檔、166/166 通過；含 Vertex cache 4 案例、診斷信任分級 25 案例（去敏、遮蔽、竄改重驗證、同源判定、五項邊界探針）、hooks 行為 14 案例（標準／`git -C`／`--git-dir <path>` 分離形式都觸發，逃生口與負案例放行）。
- Vite production build：成功；主 bundle 約 448 KB、gzip 約 147 KB。
- `npm audit --audit-level=high`：0 vulnerabilities。
- 本機 `/auris-app/settings?demo=1`：P114 版號與新診斷文案正確；切換深夜主題後 reload 仍為 `dark`；console 無 warning/error。
- 專案設定（含 NUL 位元組防線）、JSON／YAML、共用 hooks shell 語法、`git diff --check` 與版號文件防呆均通過。

### 14.5 P127 匯入安全強化復驗

- Vitest：23 個測試檔、226/226 通過；包含讀檔前容量阻擋、JPEG／PNG／WebP magic bytes、SVG／非 base64／偽造 MIME、圖片容量、JSON 文字／筆數上限、重複 key、核心欄位型別、目前教學沙盒完整資料 round-trip 與 transaction rollback。
- Vite production build：成功；project config checks 通過。
- 本機 `?demo=1`：設定頁正確顯示 P127；完整備份、單角色與聊天三條匯入入口選入損毀 JSON，皆顯示「JSON 檔案格式錯誤或已損毀」，未進入資料庫操作；console 無 warning/error。
- 版更文件防呆：進度總覽「當前版本」恰好 2 處，`APP_VERSION` 與檔頭皆為 P127；ARCHITECTURE 版本紀錄維持新→舊，Roadmap 已移除完成的 HARD-02 待辦。

### 14.6 下一批執行順序

1. main 對非管理者禁止 force push／刪除**已完成**（2026-07-15 GitHub API 復驗 `allow_force_pushes`／`allow_deletions` 皆 false）。剩：required status checks 綁定 CI——前提是先把發版改為 dev → main PR 流程，屆時一併評估 `enforce_admins`（現為 false，管理者可繞過）與 dev 保護。
2. ~~補圖片格式／大小、JSON 資源限制與 schema validator~~（P127 完成）。
3. 再補 Actions SHA pin、Dependabot、SAST、lint、coverage 與 E2E；單角色／聊天原子化維持低優先評估，不阻擋此順序。

> 每批程式修改仍須依專案 `/bump` 規則更新版號與文件，完成後跑 `/verify-app`；不得自行推送 main。
